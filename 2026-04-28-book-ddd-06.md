# Chapter 6. 응용 서비스와 표현 영역

## 6-1. 표현 영역

* 사용자의 요청을 해석
* 사용자의 requests를 받고 requests에 맞는 응용 서비스에 연결
* 응용 서비스에서 요구하는 형식으로 requests 변형
* 응용 서비스가 사용자가 REST API, 웹 브라우저, TCP 등을 이용하는지 알 필요 없게 하는 역할
  * Controller 역할을 수행한다고 생각하면 간단할 것으로 보인다.
* 사용자의 세션을 관리

## 6-2. 응용 서비스

### 6-2-1. 응용 서비스의 역할

* 사용자가 요청한 기능 수행
* 도메인 영역과 표현 영역을 연결
  * 도메인 객체를 사용해서 사용자의 요청을 처리한다.
  * 도메인 객체 간 흐름을 제어
* 트랜잭션을 담당 - 여러 도메인의 상태 변경을 컨트롤

### 6-2-2. 도메인 영역과의 관계

* 응용 서비스는 여러 도메인 객체를 이용해서 기능을 제공한다.

```java
public Result doSomeFunc(someReq req) {
  // 1. 리포지터리를 이용해 이용자 요청에 해당하는 aggregate를 가져온다.
  someAgg agg = someAggRepository.findById(req.getId());
  checkNull(agg); // 1-1. null 여부 체크
  
  // 2. 이용자 요청값과 aggregate의 도메인 기능을 이용해 원하는 역할을 수행
  agg.doFunc(req.getValue());
  
  // 3. 결과를 다시 표현 영역에 넘겨주기 적절한 형태로 변형
  //    응답 코드 포함 등
  return createSuccessResult(agg);
}
```

### 6-2-3. 응용 서비스에 도메인 로직 넣지 않기

- 도메인 로직을 응용 서비스에 구현하면 1) 코드의 응집성이 떨어지고, 2) 여러 응용 서비스에서 동일한 도메인 로직을 중복 구현할 가능성이 높음
- 중복 구현을 하지 않도록 먼저 공통화하라로도 볼 수 있는데, 또 다른 방법론에서는 중복을 처음부터 고려할 필요 없이 중복이 생기면 그때 공통화해도 충분하다고 해서, 때에 따라서 적절하게 선택해도 되지 않을까 싶다.

```java
public class ChangePasswordService {
  public void changePassword(String memberId, String oldPw, String newPw){
    // 리포지토리를 이용해 적절한 aggregate 가져온다.
    Member member = memberRepository.findById(memberId);
    // aggregate에 대한 null 체크
    // > 이건 도메인 규칙이라기 보다는 도메인을 이용하기 위한 사전 조건
    checkMemberExists(member);
    
    // 위의 코드는 이런 방식으로 하는 것이 더 DDD + java 스러울 수 있음
    // Member member = memberRepository.findById(memberId)
    // .orElseThrow(MemberNotFoundException::new);
    
    // 비밀번호 변경은 단순 setter가 아니라 Member Aggregate의 의미 있는 도메인 행위. 따라서 setPassword()보다는 changePassword() 같은 도메인 메서드를 두는 것이 더 적절
    if (!passwordEncoder.matches(oldPw, member.getPassword())) {
      throw new BadPasswordException();
    }
    member.setPassword(newPw);
  }
}
```

```java
// 위의 코드에서 Member aggregate 로직을 따로 떼어내기
public class Member {
  public void changePassword(String oldPw, string newPw) {
    if (!matchPassword(oldPw)) throw new BadPasswordException();
    setPassword(newPw);
  }
}
```

* 보통 “Rule of Three”, 즉 “세 번째 반복이 나타나면 리팩터링하라”는 원칙으로 설명
  * 대표적으로는 Martin Fowler가 『Refactoring』에서 소개했고, Fowler는 이 조언을 Don Roberts에게서 들었다고 밝힙니다. 요지는 다음과 같습니다. 처음에는 그냥 구현하고, 두 번째 비슷한 코드가 나와도 중복을 감수하며, 세 번째 비슷한 코드가 나올 때 리팩터링한다는 것입니다. 이 원칙은 “Three strikes, then you refactor”로도 알려져 있습니다.
* 따라서 “중복을 처음부터 무조건 제거하지 말고, 반복되는 패턴이 충분히 보일 때 공통화하라”는 관점의 인물로 답하면 가장 직접적으로는 다음과 같습니다.
* 그리고 비슷한 맥락에서 자주 함께 언급되는 사람은 Sandi Metz입니다. Sandi Metz는 “잘못된 추상화보다 중복이 더 싸다”는 취지로, 섣부른 공통화가 오히려 조건문과 파라미터가 덕지덕지 붙은 나쁜 추상화를 만들 수 있다고 설명합니다. 그녀는 잘못된 추상화가 확인되면 다시 중복을 도입해서 각 호출부가 실제로 필요로 하는 코드만 남기고, 그다음 다시 올바른 추상화를 찾아야 한다고 설명합니다.



## 6-3. 응용 서비스 구현

- 응용 서비스 클래스를 구현 시 
  - 한 도메인의 기능을 한 클래스에 모두 구현하는 방법
    - 장점) 각 기능에서 동일 로직이 있으면 중복 코드를 ㅈ거 가능
    - 단점) 클래스가 점점 길어져서 어떤 한 기능을 이해하는데 시간이 오래 걸릴 수 있다. -> llm과 함께라면 이 단점도 없어지는 것 같다. 
  - 기능 별로 응용 서비스 클래스를 나누는 방법이 존재
    - 한 응용 서비스 클래스에서 1~3개 기능을 구현
    - 공통 로직도 별도 클래스로 빼기
- 보통 한 도메인(e.g. 회원)에 달려있는 기능이 서비스가 진화하면서 커지기 때문에, 처음부터 분리하는 것이 깔끔할 수도 있을 것으로 보인다.

```java
class MemberChangePasswordService {
  public void changePassword(String memberId, String curPw, String newPw);
}
```



## 6-4. 값 검증

* 표현 영역과 응용 서비스 두 곳에서 모두 가능하지만, **원칙적으로 응용 서비스에서 처리**
* 사용자에게 즉각적으로 오류를 알려서 다시 request를 해야한 다는 것을 알려야 할 경우에는 표현 영역에서 처리
  * 표현 영역: 필수 값, 값의 형식, 범위 검증
  * 응용 서비스: 데이터의 존재 유무와 같은 논리적 오류 검증
* 권한 검사도 마찬가지이다. 표현, 응용, 도메인 모두에서 가능하지만, 검증을 할 때 꼭 필요한 요소가 있는 레이어까지 내려가서 처리하면 된다.
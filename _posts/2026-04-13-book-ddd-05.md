---
title: Chapter 5. 스프링 데이터 JPA를 이용한 조회 기능
date: 2026-04-13
categories: [Book, 도메인 주도 개발 시작하기]
tags: [ddd]     # TAG names should always be lowercase
author: admin
toc: true
comments: false
---

## 1. CQRS
<br/>

*명령(Command) 모델과 조회(Query) 모델을 분리하는 패턴*

- **명령 모델**: 상태를 변경하는 기능 — (예) 회원 가입, 암호 변경, 주문 취소
- **조회 모델**: 데이터를 보여주는 기능 — (예) 주문 목록, 페이징

<small>*이후 나올 Specification은 조회 모델에서 복잡한 검색 조건을 표현할 때 주로 사용하는 도구 중 하나이다.*</small>

<br/><br/>

---

## 2. 스펙 (Specification)
<br/>

*검색 조건을 다양하게 조합해야 할 때 사용하는 패턴으로, 애그리거트가 특정 조건을 충족하는지 검사하는 인터페이스*

조회 시 다양한 검색 조건이 붙게 되는 경우, 필요한 조합마다 메서드를 정의하면 유지보수해야 할 메서드 수가 계속 늘어난다.

```java
// 조건 조합에 따라 메서드가 계속 늘어남
findByStatus(String status);
findByStatusAndMinAmount(String status, long amount);
findByStatusAndMinAmountAndDateRange(...);
findByStatusAndCustomerGrade(...);
```

DDD에서 Specification을 강조하는 이유는 도메인 언어를 코드에 그대로 드러내기 위해서다. 엔티티나 밸류 오브젝트 안에 검증/필터 로직을 다 넣으면 모델이 비대해질 수 있는데, Specification으로 분리하면 애그리거트는 상태와 핵심 행위에 집중하고 "어떤 조건을 만족하는가"는 별도 객체가 담당하게 된다.

### 2-1. 핵심 인터페이스
<br/>

```java
public interface Specification<T> {
    // 애그리거트가 조건을 만족하는지 체크
    boolean isSatisfiedBy(T target);

    // 다른 조건과 and로 조합
    default Specification<T> and(Specification<T> other) {
        return t -> this.isSatisfiedBy(t) && other.isSatisfiedBy(t);
    }

    // 다른 조건과 or로 조합
    default Specification<T> or(Specification<T> other) {
        return t -> this.isSatisfiedBy(t) || other.isSatisfiedBy(t);
    }

    // 조건을 부정
    default Specification<T> not() {
        return t -> !this.isSatisfiedBy(t);
    }
}
```

### 2-2. 사용 예시
<br/>

```java
// 특정 상태를 조건으로 주어 일치하는 주문만 필터링
public class OrderStatusSpec implements Specification<Order> {
    private final String status;

    public OrderStatusSpec(String status) {
        this.status = status;
    }

    @Override
    public boolean isSatisfiedBy(Order order) {
        return order.getStatus().equals(status);
    }
}
```

조건들을 `and`로 체이닝해서 "이번 달 확정된 주문 중 10만원 이상"과 같은 복합 조건을 간결하게 표현할 수 있다.

```java
Specification<Order> spec = new OrderStatusSpec("CONFIRMED")
    .and(new MinAmountSpec(Money.of(100_000)))
    .and(new OrderDateRangeSpec(
        LocalDate.of(2026, 4, 1),
        LocalDate.of(2026, 4, 30)
    ));
```

<br/><br/>
